const config = {
  WEATHER_API_KEY: process.env.WEATHER_API_KEY,
  BASE_WEATHER_ENDPOINT: "https://api.weatherapi.com/",
  PORT: process.env.PORT,
  CORS_ALLOW_ORIGIN: process.env.CORS_ALLOW_ORIGIN,
};

export default config;
